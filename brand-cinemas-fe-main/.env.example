# Local: leave empty (or omit) so Vite proxies /api → localhost:5000 (no CORS)
# Production build uses .env.production
VITE_API_URL=

VITE_GOOGLE_CLIENT_ID=
VITE_MIDTRANS_CLIENT_KEY=
VITE_MIDTRANS_IS_PRODUCTION=false
